function changeTheme(){
    let colours = ["red", "greeen", "blue"];
    for(i=0; i < colours.length; i++){
        //console.log(colours[i]);
        document.body.style.backgroundColor = colours[i];
    }
}